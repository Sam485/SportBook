import 'package:flutter/material.dart';

class BookedDetailed extends StatelessWidget {
  const BookedDetailed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
